import { DiscoveryMode } from '../types';

export const analyticsService = {
  trackDiscoveryEndReached() {
    // Analytics tracking hooks
  },
  
  trackDiscoveryModeSelected(mode: DiscoveryMode) {
    // Analytics tracking hooks
  },
};
